const customElementName = '<%= entityFileName %>-details';
const detailsTitle = '[data-testid=details_title]';
const entityIdCell = '[data-testid=<%= entityInstance %>IdValue]';

export {
  customElementName,
  detailsTitle,
  entityIdCell
}
