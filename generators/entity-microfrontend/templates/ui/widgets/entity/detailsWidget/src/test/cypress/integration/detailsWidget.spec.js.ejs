import { customElementName, tableTitle, entityIdCell } from '../support';

describe('Main', () => {
<%- include('../../../../../_shared/src/test/cypress/integration/common.spec.js.ejs') _%>

  describe('Details widget', () => {
    it('should load the page', () => {
      cy.get(customElementName).should('exist');
    });

    it('should display the right values', () => {
      cy.get(tableTitle).should('be.visible').should('have.text', "Details about 'Foo'");
      cy.get(entityIdCell).should('not.be.empty').should('have.text', '1');
    });
  });
});
