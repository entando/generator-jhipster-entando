import { detailTestSelector } from '../support';

describe('Main', () => {
<%- include('../../../../../_shared/src/test/cypress/integration/common.spec.js.ejs') _%>

  describe('Details widget', () => {
    it('should load the page', () => {
      cy.get(detailTestSelector).should('be.visible').should('have.text', "Details about '<%= entityClassHumanized %>'");
    });
  });
});
