<%_ (languages || ['en']).forEach((lang) => { %>
import <%= lang %> from 'i18n/locales/<%= lang %>.json';<%_ }); %>

export default {
  <%_ (languages || ['en']).forEach((lang) => { _%>
  <%= lang %>,
  <%_ }); _%>
};
