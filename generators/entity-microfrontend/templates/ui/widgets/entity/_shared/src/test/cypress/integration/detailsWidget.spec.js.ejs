describe('Main', () => {
<%- include('./common.spec.js.ejs') _%>

  describe('Details widget', () => {
    it('should load the page', () => {
      cy.get('[data-testid=details_title]').should('be.visible').should('have.text', "Details about '<%= entityClassHumanized %>'");
    });
  });
});
