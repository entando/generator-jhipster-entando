<% 
    const configClassName =  `${baseName.charAt(0).toUpperCase() + baseName.substring(1)}Config`;
%>
package <%=packageName%>.config;

public class <%=configClassName%> {

    /*
        Provide here all the configuration fields your plugin require
    */

    public static <%=configClassName%> getDefault() {
        <%=configClassName%> defaultConfig = new <%=configClassName%>();
        // Add here all the default values for your configuration
        return defaultConfig;
    }
}
