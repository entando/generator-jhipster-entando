const files = {
    path: 'ui/widgets',
    templates: [
        <%_ files.map(file => { _%>
        <%_ if (file.options.skip) { _%>
        /*
        <%_ } _%>
        {
            file: '<%= file.filename %>',
            <%_ if (Object.keys(file.options).includes('renameTo')) { _%>
            renameTo: generator => <%= file.options.renameTo %>, // eslint-disable-line prettier/prettier
            <%_ } else { _%>
            renameTo: generator => `/${generator.entityInstance}<%= file.filename %>`,
            <%_ } _%>
            <%_
            Object.keys(file.options).forEach(option => {
                if (option !== 'renameTo' && option !== 'skip') {
            _%>
            <%= option %>: <%- (typeof file.options[option] === 'boolean') ? `${file.options[option]}` : `'${file.options[option]}'` %>,
            <%_
                }
            })
            _%>
        },
        <%_ if (file.options.skip) { _%>
        */
        <%_ } _%>
        <%_ }) _%>
    ],
};

module.exports = files;
