import { shape, string } from 'prop-types';

export default shape({

<%# See: entity-server/templates/src/main/java/package/common/domain/Entity.java.ejs in generator-jhipster for a fairly
complete example of processing fields %>

<% for (idx in fields) { %>

    <%# TODO this lowercasing is dirty. Would be better for the lowercasing to be a function.  %>
    <%# TODO: Map all of the js field types from the java options %>

    <%= fields[idx].fieldName%>: <%=  fields[idx].fieldType.charAt(0).toLowerCase() +  fields[idx].fieldType.slice(1) %>.isRequired,

<%}%>

});
