<%# { "useBluePrint": true } -%>
{
  "common": {
    "couldNotFetchData": "Errore durante la connessione al server",
  },
  "entities": {
    "<%= entityInstance %>": {
      "noItems": "Nessun <%= entityInstance %> disponibile",
      "id": "ID",
      <%_ fields.forEach(function(field){ _%>
      "<%= field.fieldName %>": "entities.<%= entityInstance %>.<%= field.fieldName %>",
      <%_ }) _%>
    }
  }
}
